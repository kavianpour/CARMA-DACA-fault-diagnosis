# Core scientific stack
numpy>=1.21
scipy>=1.7
scikit-learn>=1.0
pandas>=1.3
matplotlib>=3.4

# Deep learning
torch>=2.0          # CPU or CUDA build; see https://pytorch.org/get-started/locally/
# torch_geometric is installed separately because the wheel is platform-specific:
#   pip install torch_geometric
# (modern PyG provides ARMAConv / ChebConv without torch-scatter / torch-sparse)
